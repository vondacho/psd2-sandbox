# Service blueprint — screens and read models

> **Status:** proposal generated by AI. No UX research was supplied (`Q-21`), so every screen and
> every piece of copy is a hypothesis for UX review (gate G1). The screen (`SCR-*`) and read-model
> (`RM-*`) identifiers are **defined here and only here**.

The bank shows the PSU three surfaces, and `SRC-ADR` names an owner for each:

- **Ping Federate** owns the **login screen**, in the web browser;
- the **Bank ASPSP** manages the **consent screen**, where the PSU "securely and understandably grants or revokes a given TPP access to their accounts", also in the browser;
- the **bank mobile app** owns the **SCA screen**, presented on the enrolled device at the request of the CIAM, where the PSU "confirms his identity using the final authentication method".

**One surface is missing.** The ADR names a screen for granting account access and a screen for
confirming identity, but none where the PSU sees a payment's **amount and payee** before
confirming — which dynamic linking requires (`SRC-IG` §5.1.9). `Q-46` asks the bank to settle it;
until then no screen here claims that role.

## How to read it

A blueprint separates what the PSU sees (**frontstage**: screens) from what somebody needs
in order to decide (**read models**), and from who produces that information (**backstage**:
components and contexts).

A read model is the event-storming "readmodel" card: *the information an actor or a policy
needs to take one decision*. Each read model below names its consumer and decision, its data
and sources, its owning component, and the consistency it needs.

Diagrams: [`authorisation-screen-flow.puml`](../system/uml/ux/authorisation-screen-flow.puml),
[`tpp-access-screen-flow.puml`](../system/uml/ux/tpp-access-screen-flow.puml), and the
wireframes [`wf-approve-access.puml`](../system/uml/ux/wf-approve-access.puml),
[`wf-app-sca.puml`](../system/uml/ux/wf-app-sca.puml),
[`wf-tpp-access.puml`](../system/uml/ux/wf-tpp-access.puml). Component ownership: LikeC4 view
`psuScreens`.

## 1. Screens presented to the PSU

### Login screen — managed by `CMP-PING` (Ping Federate, in-house)

| ID | Screen | Shown when | Managed by | Reads | PSU decision / action | Slice | Stories |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `SCR-IDP-LOGIN` | The bank's login screen | The PSU arrives from `scaRedirect` without a bank session, for a consent **or** a payment | `CMP-PING` | — (Ping's own identity store) | Authenticate, or cancel back to the TPP | WS-01 | `STORY-SCA-REDIRECT-APP`, `STORY-PIS-SCA-REDIRECT-APP` |

### Consent screen — managed by `CMP-CONSENT-SCREEN`

A bank web application **managed by the Bank ASPSP** (`SRC-ADR`). It covers **account access** —
the ADR gives it no role in payments (`Q-46`) — plus the waiting and outcome pages that both
journeys share (`Q-48`). Where the consent *state* behind it lives is
[`D-01`](../system/decisions/d-01-consent-management.md).

| ID | Screen | Shown when | Managed by | Reads | PSU decision / action | Slice | Stories |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `SCR-CONSENT-ACCESS` | "Allow ⟨TPP⟩ to see your accounts?" | PSU authenticated; purpose = consent | `CMP-CONSENT-SCREEN` | `RM-CONSENT-APPROVAL` | **Approve** or **decline** access | WS-01 (TPP name, accounts, approve / decline); MVP-01 (all details) | `STORY-SCA-REDIRECT-APP`, `STORY-SCA-CONSENT-DETAILS` |
| `SCR-CONSENT-AWAIT-APP` | "Confirm in your bank app" — with the deadline and a way to cancel | After approve (consent), or right after login (payment, `Q-48`), until the SCA screen answers | `CMP-CONSENT-SCREEN` | `RM-AUTHORISATION-OUTCOME` (polled) | Wait, or cancel | WS-01 | `STORY-SCA-REDIRECT-APP`, `STORY-PIS-SCA-REDIRECT-APP` |
| `SCR-CONSENT-OUTCOME` | "Access granted / Payment approved / Declined / Failed", with "Back to ⟨TPP⟩" | After the SCA screen answers, or after a decline | `CMP-CONSENT-SCREEN` | `RM-AUTHORISATION-OUTCOME` | Go back to the TPP | MVP-01 (WS-01 returns immediately) | `STORY-SCA-OUTCOME` |
| `SCR-CONSENT-UNAVAILABLE` | "This request is no longer available" | Redirect token unknown, expired or used; authorisation final or past its window | `CMP-CONSENT-SCREEN` | `RM-REDIRECT-SESSION`, `RM-AUTHORISATION-OUTCOME` | Return to the TPP (nok URI) | MVP-01 | `STORY-SCA-REQUEST-UNAVAILABLE` |
| `SCR-CONSENT-NO-DEVICE` | "You need an enrolled device to confirm" | The PSU has no device enrolled with the CIAM | `CMP-CONSENT-SCREEN` | `RM-SCA-CONTEXT` | Enrol, or give up — **open** (`Q-21`, `Q-49`) | unscheduled | `STORY-SCA-NO-APP` |
| `SCR-CONSENT-MANAGE` | "What ⟨TPP⟩ can see", with "Withdraw access" | PSU reaches the consent screen to manage access (`Q-43`) | `CMP-CONSENT-SCREEN` | `RM-PSU-TPP-ACCESS` | Keep, or start withdrawing | MVP-01 | `STORY-PSU-REVOKE` |
| `SCR-CONSENT-REVOKE-CONFIRM` | "Withdraw ⟨TPP⟩'s access?" — states the consequence | PSU chooses "Withdraw access" | `CMP-CONSENT-SCREEN` | `RM-PSU-TPP-ACCESS` | Confirm or cancel | MVP-01 | `STORY-PSU-REVOKE` |

### Bank mobile app — the SCA screen

| ID | Screen | Shown when | Managed by | Reads | PSU decision / action | Slice | Stories |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `SCR-APP-SCA` | The **SCA screen**: the PSU confirms identity with the final authentication method. What else it shows — for a payment, the amount and payee — depends on `Q-46` | The CIAM asks the app to present it, on the enrolled device (`SRC-ADR`) | `CMP-MOBILE-APP`, on demand from `CMP-TRANSMIT` | `RM-SCA-PROMPT` | **Confirm** identity, or **reject** | WS-01 | `STORY-SCA-REDIRECT-APP`, `STORY-PIS-SCA-REDIRECT-APP` |
| `SCR-APP-DEVICE-ENROLMENT` | Device enrolment with the CIAM | The PSU registers a device for SCA — a bank-wide capability, not part of the XS2A journey (`A-16`) | `CMP-MOBILE-APP` + `CMP-TRANSMIT` | `RM-SCA-CONTEXT` | Enrol a device | unscheduled | `STORY-DEVICE-ENROLMENT` |

### TPP-managed screens (context only — the bank does not design them)

| ID | Screen | Managed by | What the bank influences |
| --- | --- | --- | --- |
| `SCR-TPP-REQUEST` | The TPP explains what it will access or pay, then sends the PSU to the bank | `CMP-TPP` | Nothing directly. The consent or payment it posts becomes `RM-CONSENT-APPROVAL` / `RM-PAYMENT-APPROVAL` |
| `SCR-TPP-RESULT` | The TPP shows the result after the PSU returns | `CMP-TPP` | `RM-TPP-CONSENT-STATUS`, `RM-TPP-PAYMENT-STATUS`, `psuMessage` (§5.3.1, §6.3.1.1) |

## 2. Read models

### 2.1 For the PSU's decisions (frontstage)

| ID | Consumer → decision | Data (fields) | Sources | Owner (projection / query) | Consistency | Invariants / rules |
| --- | --- | --- | --- | --- | --- | --- |
| `RM-CONSENT-APPROVAL` | PSU on `SCR-CONSENT-ACCESS` → approve or decline a TPP's access | TPP legal name; brand (`Q-38`); per account: label (`Q-37`), currency, access types; recurring or one-off; `validUntil` **as granted** (`INV-CNS-05`); `frequencyPerDay`; deadline | `CTX-CONSENT`; `CTX-TPP-ACCESS`; `CTX-CORE-BANKING` (DCP account labels, whether the PSU holds the account — `Q-35`); `CTX-TXN-AUTHORISATION` | `CMP-PSU-CHANNEL-API`, the consent-screen backend | Read at approval time, consent version pinned. The approved-subject digest (`INV-AUT-05`) comes from exactly what this model showed | R-SCA-02, `INV-AUT-05`, `INV-CNS-05` |
| `RM-PAYMENT-APPROVAL` | PSU → approve or decline the payment. **Which screen shows it is open** (`Q-46`): the ADR gives neither the consent screen nor the SCA screen that role | Amount and currency; payee name; payee account; debtor account label; remittance; execution date (`Q-13`); fees (`Q-41`); TPP legal name; VoP result (`Q-16`, later) | `CTX-PAYMENT-INITIATION`; `CTX-TPP-ACCESS`; `CTX-CORE-BANKING` | `CMP-PSU-CHANNEL-API` — inside `RM-SCA-PROMPT` if `Q-46` puts it on the SCA screen, otherwise on its own consent-screen operation | Immutable payment data (`INV-PAY-01`), so the digest is stable | R-PIS-05, `INV-AUT-05`, `INV-PAY-01`, `UX-09` |
| `RM-SCA-PROMPT` | PSU on `SCR-APP-SCA` → confirm identity, or reject | TPP legal name; purpose; what is being confirmed; request time; approved-subject digest. Whether it also carries the payment's amount and payee depends on `Q-46` | `CTX-TXN-AUTHORISATION`; `RM-PAYMENT-APPROVAL` if `Q-46` puts payment details here | `CMP-PSU-CHANNEL-API`, reached by the app when the CIAM triggers the screen (`Q-45`) | Strong: must match the subject the PSU approved | `INV-AUT-05`, `UX-01` |
| `RM-AUTHORISATION-OUTCOME` | PSU on `SCR-CONSENT-AWAIT-APP`, `-OUTCOME`, `-UNAVAILABLE` → wait, or go back to the TPP | Purpose; outcome (pending SCA, approved, declined, failed, expired, already handled); TPP legal name; return target (`INV-AUT-03`) | `CTX-TXN-AUTHORISATION` | `CMP-AUTHORISATION`, served by the consent-screen backend | Strong: reflects the decision and the SCA result | R-SCA-03, R-SCA-04 |
| `RM-PSU-TPP-ACCESS` | PSU on `SCR-CONSENT-MANAGE` / `-REVOKE-CONFIRM` → keep or withdraw **this TPP's** access | This PSU's consents for this TPP: TPP name; accounts and access types; granted on; valid until; recurring; last read (`Q-40`); status | `CTX-CONSENT`; `CTX-TPP-ACCESS`; `CTX-CORE-BANKING`; `CTX-ACCOUNT-INFORMATION` | `CMP-CONSENT`, served by the consent-screen backend | Read-your-writes: a withdrawn access shows "withdrawn" immediately | R-REV-01, R-REV-04, `INV-CNS-04` |

### 2.2 For components and policies (backstage)

| ID | Consumer → decision | Data | Sources | Owner | Consistency | Invariants / rules |
| --- | --- | --- | --- | --- | --- | --- |
| `RM-REDIRECT-SESSION` | `CMP-CONSENT-SCREEN` → is this link valid, which authorisation and purpose; continue or show `SCR-CONSENT-UNAVAILABLE` | Opaque single-use redirect token → authorisationId, purpose, scaStatus, deadline, used flag, return URIs | `CTX-TXN-AUTHORISATION` | `CMP-AUTHORISATION` | Strong; single use enforced atomically | `INV-AUT-01`, `INV-AUT-03` |
| `RM-SCA-CONTEXT` | `CMP-TRANSMIT` → which enrolled device to ask, and what to bind; `CMP-CONSENT-SCREEN` → is there an enrolled device at all | PSU identity (from Ping); enrolled devices; approved-subject digest; amount and payee | `CTX-CUSTOMER-IDENTITY` (Ping identity, Transmit devices); `CMP-AUTHORISATION` (digest) | `CTX-CUSTOMER-IDENTITY` | Strong | `INV-AUT-05` |
| `RM-ACCESS-DECISION` | `CMP-ACCOUNT-INFO` → serve or refuse an account read | Consent status; validUntil; owning TPP; access per account; frequencyPerDay; today's unattended reads | `CTX-CONSENT`; Daily Access Allowance | `CMP-CONSENT` + `CMP-ACCOUNT-INFO` (`D-01` decides whether this is bank-local) | **Strong on revocation** (`H-03`) — the deciding criterion in `D-01` | `INV-CNS-06`, `INV-ACC-03` |
| `RM-SCA-DEADLINES` | Consent and payment timeout policies → reject when the SCA window closed | authorisationId, subject, started at, deadline, scaStatus | `CTX-TXN-AUTHORISATION` | `CMP-AUTHORISATION` | Eventual (scheduled) | `INV-PAY-03`; window `Q-29` |
| `RM-CONSENTS-DUE-TO-EXPIRE` | Consent expiry policy → expire a consent | consentId, validUntil, recurring, one-off used | `CTX-CONSENT` | `CMP-CONSENT` | Eventual (scheduled) | `INV-CNS-01` |

### 2.3 For the TPP's decisions (exposed through `API-XS2A-PROFILE`)

| ID | Consumer → decision | Operations | Data |
| --- | --- | --- | --- |
| `RM-TPP-CONSENT-STATUS` | AISP → start reading data, or ask the PSU again | `getConsentStatus`, `getConsentInformation` | consentStatus, access and validity as granted |
| `RM-TPP-SCA-STATUS` | AISP / PISP → keep waiting, or give up | `getConsentScaStatus`, `getPaymentInitiationScaStatus` | scaStatus |
| `RM-TPP-PAYMENT-STATUS` | PISP → tell the merchant and the PSU the outcome | `getPaymentInitiationStatus` | transactionStatus, fundsAvailable, tppMessages |
| `RM-TPP-ACCOUNTS` | AISP → which accounts to read | `getAccountList` | Accounts with resourceId and links |

### 2.4 For the bank's own decisions

| ID | Consumer → decision | Data | Source | Evidence |
| --- | --- | --- | --- | --- |
| `RM-OPS-SCA-FUNNEL` | Product owner → is the MVP hypothesis confirmed (`H-01`)? | Per TPP and purpose: arrived, logged in, approved, declined, SCA screen shown, confirmed, rejected, expired; time from approve to confirm | `API-XS2A-EVENTS` + web and app analytics (`Q-22`) | `EVID-MVP-SCA-CONVERSION` |
| `RM-OPS-XS2A-HEALTH` | Bank operations → open an incident, or escalate to Finologee / DCP | Per operation: rate, p95 latency, error codes, availability | Traces and metrics (`CMP-OBSERVABILITY`) | `EVID-MVP-OPS` |

## 3. Blueprint per journey

### `JRN-AIS-CONNECT` — grant account access

| Stage | PSU sees | Read model | Managed by | Backstage |
| --- | --- | --- | --- | --- |
| S2 | `SCR-TPP-REQUEST` | — | TPP | `createConsent`; authorisation started implicitly |
| S3 | `SCR-IDP-LOGIN` | — | `CMP-PING` | Redirect session resolved; PSU authenticated |
| S3 | `SCR-CONSENT-ACCESS` | `RM-CONSENT-APPROVAL` | `CMP-CONSENT-SCREEN` | Approve / decline recorded with the subject digest |
| S3 | `SCR-CONSENT-AWAIT-APP` (browser) **and** `SCR-APP-SCA` (app, on the enrolled device) | `RM-AUTHORISATION-OUTCOME`; `RM-SCA-PROMPT` | `CMP-CONSENT-SCREEN`; `CMP-MOBILE-APP` triggered by `CMP-TRANSMIT` | SCA result → "Record SCA result" → "Authorisation finalised / failed" |
| S3 | `SCR-CONSENT-OUTCOME` | `RM-AUTHORISATION-OUTCOME` | `CMP-CONSENT-SCREEN` | Consent valid / rejected |
| S4 | `SCR-TPP-RESULT` | `RM-TPP-CONSENT-STATUS` | TPP | Status reads |

### `JRN-PIS-PAY` — authorise a payment

`SRC-ADR` gives the consent screen no payment page, and describes the SCA screen as confirming
*identity*. So the surface where the PSU sees the amount and payee — which dynamic linking
requires — is **not yet defined** (`Q-46`).

| Stage | PSU sees | Read model | Managed by | Backstage |
| --- | --- | --- | --- | --- |
| S1 | `SCR-TPP-REQUEST` | — | TPP | `initiatePayment`; authorisation started implicitly |
| S2 | `SCR-IDP-LOGIN` | — | `CMP-PING` | Redirect session resolved; PSU authenticated |
| S2 | **The surface that shows amount and payee is undecided (`Q-46`)** | `RM-PAYMENT-APPROVAL` | — | Dynamic linking needs the PSU to see what is being paid |
| S2 | `SCR-CONSENT-AWAIT-APP` (browser, `Q-48`) **and** `SCR-APP-SCA` | `RM-AUTHORISATION-OUTCOME`; `RM-SCA-PROMPT` | `CMP-CONSENT-SCREEN`; `CMP-MOBILE-APP` | Confirm → authorisation finalised → payment submitted to DCP |
| S2 | `SCR-CONSENT-OUTCOME` | `RM-AUTHORISATION-OUTCOME` | `CMP-CONSENT-SCREEN` | — |
| S3 | `SCR-TPP-RESULT` | `RM-TPP-PAYMENT-STATUS` | TPP | Status reads |

### `JRN-ACCESS-CONTROL` — review and withdraw one TPP's access

| Stage | PSU sees | Read model | Managed by | Backstage |
| --- | --- | --- | --- | --- |
| S1 | `SCR-IDP-LOGIN` → `SCR-CONSENT-MANAGE` (`Q-43`) | `RM-PSU-TPP-ACCESS` | `CMP-PING`; `CMP-CONSENT-SCREEN` | This PSU's consents for this TPP |
| S2 | `SCR-CONSENT-REVOKE-CONFIRM` → back to `SCR-CONSENT-MANAGE`, shown as withdrawn | `RM-PSU-TPP-ACCESS` | `CMP-CONSENT-SCREEN` | "Consent revoked by the PSU"; `RM-ACCESS-DECISION` refuses the TPP's next read |

## 4. UX requirements proposed (hypotheses for UX review)

| ID | Proposed requirement | Why | Evidence to collect |
| --- | --- | --- | --- |
| `UX-01` | What is bound by SCA is shown where the PSU confirms: the consent screen for access, and — once `Q-46` is settled — the amount and payee for a payment | Dynamic linking (`INV-AUT-05`); §6 detailed consent | Security review; usability sessions |
| `UX-02` | **Decline is as easy and visible as approve**, on the consent screen *and* on the SCA screen | The PSU's control (`OBJ-02`) | Usability sessions |
| `UX-09` | A payment is never confirmed on a screen that does not show its amount and payee | Dynamic linking; `Q-46` | Security and compliance review |
| `UX-03` | The TPP is named as the PSU would recognise it; the bank's identity is unmistakable on the login, consent and SCA screens | `H-02`, §4.9, `Q-47` | Usability sessions; phishing review |
| `UX-04` | Validity and frequency in plain language | Consent fields are technical (§6.3.1.1) | Comprehension test |
| `UX-05` | Every dead end offers a way back to the TPP: expired, used, failed, declined, no enrolled device | TPP-Nok-Redirect-URI (§6.3.1.1) | `RM-OPS-SCA-FUNNEL` |
| `UX-06` | Withdrawing access states the consequence and takes effect immediately at the bank | `H-03`; `RM-ACCESS-DECISION` | Usability sessions; contract test R-REV-02 |
| `UX-07` | No bank marketing on the login, consent or SCA screens | Keep the decision focused | UX review |
| `UX-08` | The hand-over from browser to app is explicit: the waiting page says which device was asked, shows the deadline, and updates itself when the app answers | Three surfaces, often two devices (`H-01`, `RSK-06`) | Funnel: approve → confirm drop-off |

## 5. Accessibility

The standard is not supplied (`Q-42`). The baseline applies to all three bank surfaces — login
screen, consent screen and SCA screen:

- screen-reader labels for amounts, IBANs and dates;
- dynamic type without truncating amounts, payees or TPP names;
- contrast and target sizes per the bank's standard;
- an alternative to biometrics on `SCR-APP-SCA` (CIAM methods include OTP, `SRC-ADR`);
- no time pressure without warning: show the SCA window (`Q-29`) on the waiting page and in the app.

## 6. Questions this blueprint depends on

Screen content: `Q-35` (an account the PSU does not hold), `Q-36` (partial approval), `Q-37`
(account labels), `Q-38` (TPP brand), `Q-39` (outcome screen), `Q-40` (last read), `Q-41` (fees).
Journey shape: `Q-43` (how the PSU reaches the consent screen), `Q-46` and `Q-48` (payments),
`Q-47` (domain and branding), `Q-49` (device enrolment as a precondition), `Q-21` (no enrolled
device), `Q-42` (accessibility). All are in the
[ledger](../traceability/questions-and-assumptions.md).
